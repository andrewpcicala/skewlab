// A real desk would source both of these live from a yield curve and a
// dividend futures strip. These are deliberate simplifications.

// Approximated from 3-month T-bill yield at implementation time.
export const RISK_FREE_RATE = 0.045;

// Rough SPY trailing 12-month dividend yield.
export const DIV_YIELD_DEFAULT = 0.012;
